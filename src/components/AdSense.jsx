import React from 'react';

// Simplified AdSense component for future manual ad placement
// Currently using auto ads in index.html
const AdSense = ({ 
  adSlot, 
  adFormat = 'auto', 
  style = {}, 
  className = '',
  responsive = true
}) => {
  // Since we're using auto ads, this component is not actively used
  // Keeping it for future manual ad placement if needed
  return null;
};

export default AdSense; 